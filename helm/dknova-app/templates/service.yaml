apiVersion: v1
kind: Service
metadata:
  name: {{ include "dknova-app.fullname" . }}
  labels:
    {{- include "dknova-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dknova-app.selectorLabels" . | nindent 4 }}
