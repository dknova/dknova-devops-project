apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dknova-app.fullname" . }}-test-connection"
  labels:
    {{- include "dknova-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dknova-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
